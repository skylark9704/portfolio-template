import { WheelDirective } from './wheel.directive';

describe('WheelDirective', () => {
  it('should create an instance', () => {
    const directive = new WheelDirective();
    expect(directive).toBeTruthy();
  });
});
